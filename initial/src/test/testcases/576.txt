float foo(float a, float b){
            /* comment
            */
            if(a == 1 && a > 0)
                return b;
            return foo(a-1, a*b);
        }
        void main(){
            // tail call opt
            putFloat(foo(8, 1));
        }